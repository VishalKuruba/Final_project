apiVersion: v1
kind: Service
metadata:
  name: {{ include "classifier-app.fullname" . }}
  labels:
    {{- include "classifier-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "classifier-app.selectorLabels" . | nindent 4 }}
